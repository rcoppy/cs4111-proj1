export default class User {
    constructor({ birthday, firstName, lastName, sex, id }) {
        this.birthday = birthday; 
        this.firstName = firstName; 
        this.lastName = lastName; 
        this.sex = sex; 
        this.id = id; 
    }
}